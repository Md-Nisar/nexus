{
  "mcpServers": {
    "sonarcloud": {
      "serverUrl": "https://api.sonarcloud.io/mcp",
      "headers": {
        "Authorization": "Bearer ${SONAR_TOKEN}",
        "SONARQUBE_ORG": "Md-Nisar"
      }
    }
  }
}
